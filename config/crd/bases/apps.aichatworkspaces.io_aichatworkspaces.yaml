---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: aichatworkspaces.apps.aichatworkspaces.io
spec:
  group: apps.aichatworkspaces.io
  names:
    kind: AIChatWorkspace
    listKind: AIChatWorkspaceList
    plural: aichatworkspaces
    singular: aichatworkspace
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AIChatWorkspace is the Schema for the aichatworkspaces API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AIChatWorkspaceSpec defines the desired state of AIChatWorkspace.
            properties:
              embeddings:
                description: List of default embedding models
                items:
                  type: string
                type: array
              flowise:
                description: |-
                  Flowise another No/low code AI agent builder that pairs very well with n8n
                  https://flowiseai.com/
                type: boolean
              models:
                description: List of default models for this workspace.
                items:
                  type: string
                type: array
              n8n:
                description: N8N is a low-code platform with over 400 integrations
                  and AI components
                type: boolean
              openwebui:
                description: |-
                  OpenwebUI
                  https://openwebui.com/
                type: boolean
              openwebuiPipelines:
                description: |-
                  Openwebui Pipelines
                  Modular, customizable workflows for any UI client supporting OpenAI API specs
                type: boolean
              patterns:
                description: |-
                  List of patterns
                  https://github.com/danielmiessler/fabric/tree/main/patterns
                items:
                  type: string
                type: array
              postgres:
                description: |-
                  timescale postgres with pgai support
                  https://docs.timescale.com/
                type: boolean
              qdrant:
                description: |-
                  Qdrant an open-source, high performance vector store with an comprehensive API
                  https://qdrant.tech/
                type: boolean
              workspaceENV:
                type: string
              workspaceName:
                description: The name of the workspace.
                type: string
                x-kubernetes-validations:
                - message: WorkspaceName is immutable
                  rule: self == oldSelf
            required:
            - models
            - workspaceENV
            - workspaceName
            type: object
          status:
            description: AIChatWorkspaceStatus defines the observed state of AIChatWorkspace.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              isCreated:
                description: Used to make decision during the reconcile.
                type: boolean
              models:
                description: Which model is currently loaded
                items:
                  type: string
                type: array
              ollamaWorkload:
                description: PodInfo name, cpu and memory usage and diskusage
                type: string
              openwebuiWorkload:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
